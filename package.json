{
  "name": "scrapbook",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @sentry/scrapbook build && pnpm --filter @sentry/scrapbook-jest --filter @sentry/scrapbook-vitest build",
    "dev": "pnpm -r dev",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/getsentry/scrapbook.git"
  },
  "version": "0.1.0"
}
